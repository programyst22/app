import type { GlbProps } from "./GlbModel";

/** GLB loading on native requires a dev build with asset polyfills; the digital twin falls back to the zone-block model there. */
export default function GlbModel(_props: GlbProps) {
  return null;
}
